// Package models defines the data structures persisted and exchanged
// by the lookup service: the per-address Registration record and the
// service's own Identity (signing key, party, published keys).
package models
